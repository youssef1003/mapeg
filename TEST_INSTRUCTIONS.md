# تعليمات الاختبار

## المشاكل التي تم إصلاحها:

### 1. ✅ حماية APIs الإدارية
- تم إضافة التحقق من تسجيل الدخول لجميع APIs الإدارية
- الآن يجب أن تكون مسجل دخول كأدمن للوصول إلى:
  - `/api/admin/about-settings` (GET & POST)
  - `/api/admin/blog-settings` (GET & POST)
  - `/api/admin/blog-settings/[id]` (GET, PUT, DELETE)

### 2. ✅ إضافة Logging للحفظ
- تم إضافة console.log لتتبع عملية الحفظ
- يمكنك الآن رؤية ما يحدث في terminal عند الحفظ

### 3. ✅ تحسين Middleware
- تم تحسين التحقق من الـ cookies
- الآن يتحقق من القيم الفعلية وليس فقط وجود الـ cookie

## كيفية الاختبار:

### اختبار 1: تسجيل الدخول
1. افتح المتصفح في وضع Incognito/Private
2. اذهب إلى: `http://localhost:3000/ar/auth/login`
3. أدخل بيانات الأدمن من `.env`:
   - Email: من `ADMIN_EMAIL`
   - Password: من `ADMIN_PASSWORD`
4. اضغط "تسجيل الدخول"
5. **المتوقع**: يجب أن يتم توجيهك إلى `/ar/admin`

### اختبار 2: حفظ إعدادات من نحن
1. بعد تسجيل الدخول، اذهب إلى: `/ar/admin/about-settings`
2. غير أي نص (مثلاً: عنوان Hero)
3. اضغط "حفظ التغييرات"
4. **المتوقع**: 
   - رسالة "✅ تم حفظ الإعدادات بنجاح"
   - في terminal ترى logs مثل:
     ```
     Received data: {...}
     Updating content...
     Content updated successfully
     All updates completed successfully
     ```
5. اذهب إلى `/ar/about` وتأكد من ظهور التغييرات

### اختبار 3: إضافة مقال في المدونة
1. اذهب إلى: `/ar/admin/blog-settings`
2. اضغط "➕ مقال جديد"
3. املأ جميع الحقول (عربي وإنجليزي)
4. اضغط "💾 حفظ المقال"
5. **المتوقع**:
   - رسالة "✅ تم حفظ المقال بنجاح"
   - المقال يظهر في القائمة
   - في terminal ترى: `Creating blog post:` و `Blog post created successfully:`
6. اذهب إلى `/ar/blog` وتأكد من ظهور المقال

### اختبار 4: الحماية (بدون تسجيل دخول)
1. افتح متصفح جديد في وضع Incognito
2. حاول الوصول مباشرة إلى: `/ar/admin/about-settings`
3. **المتوقع**: يتم توجيهك إلى `/ar/auth/admin-login`

### اختبار 5: منع الدخول المزدوج
1. سجل دخول كأدمن
2. حاول الذهاب إلى: `/ar/auth/login`
3. **المتوقع**: يتم توجيهك تلقائياً إلى `/ar` (الصفحة الرئيسية)

## إذا لم يعمل الحفظ:

### تحقق من Terminal:
1. شغل السيرفر: `npm run dev`
2. افتح terminal وراقب الـ logs
3. عند الضغط على "حفظ"، يجب أن ترى:
   ```
   Received data: {...}
   Updating content...
   Content updated successfully
   ```

### إذا رأيت خطأ "Unauthorized":
- معناه أنك غير مسجل دخول كأدمن
- سجل خروج ثم سجل دخول مرة أخرى
- تأكد من استخدام بيانات الأدمن الصحيحة من `.env`

### إذا رأيت خطأ في قاعدة البيانات:
- تأكد من أن قاعدة البيانات تعمل: `npm run dev` (يجب أن يتصل بـ PostgreSQL)
- جرب: `npx prisma generate` ثم `npm run dev`

## ملاحظات مهمة:

1. **يجب تسجيل الدخول كأدمن** للوصول إلى صفحات الإعدادات
2. **التغييرات تحفظ فوراً** في قاعدة البيانات
3. **لا حاجة لإعادة تشغيل السيرفر** بعد الحفظ
4. **راقب Terminal** لرؤية ما يحدث خلف الكواليس

## إذا استمرت المشكلة:

1. امسح الـ cookies من المتصفح
2. سجل خروج ثم سجل دخول مرة أخرى
3. تأكد من أن `.env` يحتوي على:
   ```
   ADMIN_EMAIL=your-email@example.com
   ADMIN_PASSWORD=your-secure-password
   ```
4. أعد تشغيل السيرفر: `npm run dev`
