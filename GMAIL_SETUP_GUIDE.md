# دليل إعداد Gmail للإيميلات

## الخطوات بالتفصيل:

### 1. تفعيل Two-Step Verification

1. روح على: https://myaccount.google.com/security
2. دور على "2-Step Verification" أو "التحقق بخطوتين"
3. لو مش مفعّل، اضغط عليه وفعّله
4. هيطلب منك رقم تليفون - حط رقمك واستلم كود التأكيد

### 2. إنشاء App Password

1. بعد ما تفعّل 2-Step Verification، ارجع لـ: https://myaccount.google.com/security
2. دور على "App passwords" أو "كلمات مرور التطبيقات"
3. اضغط عليها
4. هيطلب منك تسجيل دخول تاني - سجل دخول
5. في الصفحة اللي هتفتح:
   - Select app: اختار "Mail"
   - Select device: اختار "Other" واكتب "MapEg Website"
6. اضغط "Generate"
7. هيظهرلك كلمة مرور من 16 حرف (مثال: abcd efgh ijkl mnop)
8. **انسخها دلوقتي** - مش هتقدر تشوفها تاني!

### 3. إضافة المتغيرات في ملف .env المحلي

افتح ملف `.env` عندك وضيف السطور دي:

```env
# Email Configuration (SMTP)
SMTP_HOST="smtp.gmail.com"
SMTP_PORT="465"
SMTP_USER="your-email@gmail.com"
SMTP_PASS="abcd efgh ijkl mnop"
SMTP_FROM="MapEg <noreply@mapeg.com>"
```

**استبدل:**
- `your-email@gmail.com` ببريدك الحقيقي
- `abcd efgh ijkl mnop` بكلمة المرور اللي نسختها (احذف المسافات أو سيبها)

### 4. إضافة المتغيرات في Vercel (للموقع الحقيقي)

1. روح على: https://vercel.com/dashboard
2. اختار مشروعك (mapeg)
3. اضغط على "Settings" من القائمة العلوية
4. اضغط على "Environment Variables" من القائمة الجانبية
5. ضيف كل متغير لوحده:

   **المتغير الأول:**
   - Key: `SMTP_HOST`
   - Value: `smtp.gmail.com`
   - اضغط Add

   **المتغير الثاني:**
   - Key: `SMTP_PORT`
   - Value: `465`
   - اضغط Add

   **المتغير الثالث:**
   - Key: `SMTP_USER`
   - Value: `your-email@gmail.com` (بريدك)
   - اضغط Add

   **المتغير الرابع:**
   - Key: `SMTP_PASS`
   - Value: `abcdefghijklmnop` (كلمة المرور من 16 حرف بدون مسافات)
   - اضغط Add

   **المتغير الخامس:**
   - Key: `SMTP_FROM`
   - Value: `MapEg <noreply@mapeg.com>`
   - اضغط Add

6. بعد ما تضيف كل المتغيرات، Vercel هيطلب منك تعمل Redeploy
7. اضغط "Redeploy" عشان التغييرات تتطبق

### 5. اختبار الإيميلات

**على جهازك (محلياً):**
```bash
npm run dev
```
- روح على الموقع وسجل حساب جديد
- شوف الـ terminal - هتلاقي رابط التأكيد مطبوع
- انسخ الرابط وافتحه في المتصفح

**على الموقع الحقيقي:**
- روح على موقعك على Vercel
- سجل حساب جديد ببريد حقيقي
- افتح بريدك وشوف الإيميل
- اضغط على رابط التأكيد

## ⚠️ ملاحظات مهمة:

1. **الأمان:** كلمة المرور دي خاصة بالتطبيق فقط - لو حد أخذها مش هيقدر يدخل على Gmail بتاعك
2. **الحد اليومي:** Gmail بيسمح بـ 500 إيميل في اليوم للحسابات المجانية
3. **Spam:** أول مرة ممكن الإيميلات تروح Spam - قول للمستخدمين يتأكدوا من مجلد Spam
4. **البريد المرسل:** لو عايز تغير البريد اللي بيظهر للمستخدمين، غير `SMTP_FROM`

## 🔧 استكشاف الأخطاء:

### الإيميل مش بيوصل:
- تأكد إن كلمة المرور صح (بدون مسافات)
- تأكد إن 2-Step Verification مفعّل
- شوف الـ Vercel Logs للأخطاء

### "Invalid credentials":
- كلمة المرور غلط - اعمل App Password جديد

### "Less secure app":
- Gmail مش بيقبل "Less secure apps" دلوقتي - لازم تستخدم App Password

## ✅ Checklist:

- [ ] فعّلت 2-Step Verification على Gmail
- [ ] عملت App Password
- [ ] ضفت المتغيرات في `.env` المحلي
- [ ] ضفت المتغيرات في Vercel
- [ ] عملت Redeploy على Vercel
- [ ] جربت التسجيل والإيميل وصل
