# دليل اختبار نظام الترجمة 🧪

## كيفية اختبار النظام الجديد

### 1️⃣ تشغيل المشروع
```bash
npm run dev
```

### 2️⃣ تسجيل الدخول كمسؤول
1. اذهب إلى: `http://localhost:3000/ar/auth/login`
2. استخدم بيانات المسؤول من ملف `.env`:
   - البريد الإلكتروني: من `ADMIN_EMAIL`
   - كلمة المرور: من `ADMIN_PASSWORD`

### 3️⃣ إضافة وظيفة جديدة بترجمة
1. من لوحة التحكم، اضغط على "💼 الوظائف"
2. اضغط "إضافة وظيفة جديدة"
3. **في تبويب 🇪🇬 العربية:**
   - املأ: مسمى الوظيفة (مثال: "مهندس برمجيات")
   - املأ: وصف الوظيفة
   - املأ: المتطلبات والمهارات
   - املأ باقي الحقول (الشركة، الموقع، إلخ)
4. **اضغط على تبويب 🇬🇧 English:**
   - املأ: Job Title (مثال: "Software Engineer")
   - املأ: Job Description
   - املأ: Requirements
5. فعّل "وظيفة مميزة" ✓
6. اضغط "نشر الوظيفة"

### 4️⃣ اختبار العرض بالعربي
1. اذهب إلى: `http://localhost:3000/ar`
2. يجب أن ترى الوظيفة في قسم "الوظائف المميزة"
3. يجب أن يظهر العنوان بالعربي: "مهندس برمجيات"
4. اذهب إلى: `http://localhost:3000/ar/jobs`
5. يجب أن ترى الوظيفة في القائمة بالعربي

### 5️⃣ اختبار العرض بالإنجليزي
1. اذهب إلى: `http://localhost:3000/en`
2. يجب أن ترى الوظيفة في قسم "Featured Jobs"
3. يجب أن يظهر العنوان بالإنجليزي: "Software Engineer"
4. اذهب إلى: `http://localhost:3000/en/jobs`
5. يجب أن ترى الوظيفة في القائمة بالإنجليزي

### 6️⃣ اختبار Fallback (بدون ترجمة)
1. أضف وظيفة جديدة
2. املأ فقط الحقول العربية
3. لا تملأ الحقول الإنجليزية
4. احفظ الوظيفة
5. اذهب إلى `/en/jobs`
6. يجب أن ترى الوظيفة بالنص العربي (لأن الترجمة غير موجودة)

### 7️⃣ اختبار التعديل
1. من لوحة التحكم، اذهب إلى "الوظائف"
2. اضغط على ✏️ بجانب أي وظيفة
3. يجب أن ترى تبويبات اللغة
4. عدّل النصوص في أي لغة
5. احفظ التعديلات
6. تحقق من التغييرات في `/ar/jobs` و `/en/jobs`

---

## ✅ النتائج المتوقعة

### في الصفحة العربية (`/ar`):
- ✓ جميع عناوين الوظائف بالعربي
- ✓ جميع الأوصاف بالعربي
- ✓ جميع المتطلبات بالعربي

### في الصفحة الإنجليزية (`/en`):
- ✓ إذا كانت الترجمة موجودة → تظهر بالإنجليزي
- ✓ إذا لم تكن موجودة → تظهر بالعربي (fallback)

### في لوحة التحكم:
- ✓ تبويبات اللغة تعمل بشكل صحيح
- ✓ يمكن التبديل بين العربي والإنجليزي
- ✓ الحفظ يعمل لكلا اللغتين
- ✓ التعديل يحمل الترجمات الموجودة

---

## 🐛 إذا واجهت مشكلة

### المشكلة: الوظائف لا تظهر في الصفحة الرئيسية
**الحل:** تأكد من تفعيل "وظيفة مميزة" عند إضافة الوظيفة

### المشكلة: الترجمة لا تظهر في الصفحة الإنجليزية
**الحل:** 
1. تأكد من ملء الحقول الإنجليزية عند إضافة الوظيفة
2. تحقق من قاعدة البيانات أن الحقول `titleEn`, `descriptionEn`, `requirementsEn` ليست NULL

### المشكلة: خطأ عند الحفظ
**الحل:**
1. تأكد من ملء جميع الحقول المطلوبة (العربية)
2. الحقول الإنجليزية اختيارية، يمكن تركها فارغة

---

## 📊 قاعدة البيانات

للتحقق من البيانات في قاعدة البيانات:

```bash
npx prisma studio
```

ثم افتح جدول `Job` وتحقق من:
- `title` - العنوان بالعربي
- `titleEn` - العنوان بالإنجليزي (قد يكون null)
- `description` - الوصف بالعربي
- `descriptionEn` - الوصف بالإنجليزي (قد يكون null)
- `requirements` - المتطلبات بالعربي
- `requirementsEn` - المتطلبات بالإنجليزي (قد يكون null)

---

## 🎉 النظام جاهز!

إذا نجحت جميع الاختبارات أعلاه، فالنظام يعمل بشكل مثالي! 🚀
